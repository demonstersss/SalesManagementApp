<Window x:Class="VegetableShopApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Магазин овощей и фруктов" Height="600" Width="1000">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="C:\Users\Demonsters\source\repos\Shop\Shop\bin\Debug\net9.0-windows\im.jpg" Stretch="UniformToFill" />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="Основные">
                <MenuItem Header="Добавить количество" Click="AddQuantity_Click"/>
                <MenuItem Header="Продать" Click="SellProduct_Click"/>
            </MenuItem>
            <MenuItem Header="Справочник">
                <MenuItem Header="Вес продуктов" Click="OpenWeights_Click"/>
                <MenuItem Header="Состав продуктов" Click="OpenCompositions_Click"/>
                <MenuItem Header="Условия хранения" Click="OpenStorage_Click"/>
                <MenuItem Header="История продаж" Click="OpenSales_Click"/>
            </MenuItem>
        </Menu>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,10,10,5">
            <TextBox x:Name="SearchTextBox" 
                     Width="300" 
                     Height="25" 
                     VerticalContentAlignment="Center"
                     ToolTip="Введите часть названия продукта"/>
            <Button Content="Поиск" 
                    Margin="10,0,0,0" 
                    Padding="10,0" 
                    Click="SearchButton_Click"/>
            <Button Content="Сбросить" 
                    Margin="10,0,0,0" 
                    Padding="10,0" 
                    Click="ResetSearch_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2" 
                  x:Name="ProductsDataGrid" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  Margin="10"
                  Background="Transparent">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="120"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat=C}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>